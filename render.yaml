services:
  - type: web
    name: certfivate-maker
    env: python
    pythonVersion: 3.11
    buildCommand: pip install -r requirements.txt
    startCommand: cd web && gunicorn -w 4 -b 0.0.0.0:$PORT app:app
